import { breakpoints } from './vars/breakpoints';
import { fonts, fontSizes, fontWeights } from './vars/fonts';
import { spacing } from './vars/spacing';
import { typography } from './vars/typography';

export const baseTheme = {
  breakpoints,
  fonts,
  fontSizes,
  fontWeights,
  spacing,
  typography,
};
